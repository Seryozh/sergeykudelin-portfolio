# Self-prompt: start at the repair

Rebuild Sergey Kudelin's portfolio after a hostile review rejected the reusable `Field Entry` system.

There is no portfolio-wide visual metaphor. Start on the strongest specific product contradiction Sergey can currently defend: at Fyxed, the rent-timing story did not resolve the priced repair. The owner-paid financing idea is a proposed product and GTM thesis. Its contractual path, accounting treatment, and market response are still unverified.

## The opening

The first screen is already the Fyxed case. Do not create a hero above it.

Make the actor relationship become the composition. The property-manager rent path runs horizontally. A priced repair interrupts it as a vertical block. Sergey's owner question begins on the other side. The repair is conceptual and never impersonates an invoice.

Use one interaction only:

1. The repair interrupts the inherited route on first render.
2. `Move the cost choice to the owner` reveals Sergey's proposed question.
3. `Show what the thesis still has to prove` reveals the operating and market tests.

Do not expose step counters, state labels, legends, nodes, case numbers, coordinates, or design-system language.

## FutureClinic

FutureClinic must not reuse the Fyxed interaction or layout. Present it as a plain, source-bounded piece of writing. State the product and build role, explain the current workflow idea, and say which proof is still missing. Do not fabricate an artifact to make the section feel complete.

## Visual rule

Use white, black, one neutral, and cobalt for Sergey's proposed correction. The inherited route is ordinary black. Remove orange, paper simulation, grain, fold shadows, 3D perspective, panels, and mono typography.

Typography is IBM Plex Sans throughout. Type changes scale only when the actual sentence needs to change the composition.

## Progressive enhancement

Server-render the complete Fyxed thesis and all unknowns. Keep the control hidden in the base state. After hydration, mark the section as enhanced and begin at the interrupted route. If JavaScript fails, the visitor gets the complete static case and no inert control.

The control is a multi-step action, not a toggle or disclosure. Give it an action-specific accessible name and connect it to a polite live status. Do not use `aria-pressed` or `aria-expanded`.

Print shows the complete static case. Reduced motion makes the reroute immediate. The main skip target receives programmatic focus.

## Refusals

- No generic hero.
- No reusable case-study shell.
- No public taxonomy or provenance theater.
- No fake product UI, repair invoice, metric, quote, or evidence ID.
- No gradients, rounded cards, bento grid, display serif, mono labels, scroll reveal, parallax, custom cursor, or decorative motion.
- No rule-of-three marketing phrasing.

## Failure test

The opening fails if its text can be swapped for another designer's case without changing the composition. FutureClinic fails if missing proof is disguised with a schematic. The whole page fails if the proposed Fyxed thesis reads as an existing product mechanism.
