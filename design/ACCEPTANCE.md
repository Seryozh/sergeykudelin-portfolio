# Acceptance contract

Score each line 0, 1, or 2. The build needs at least 18 of 20. A zero on specificity, product truth, case difference, refusals, or access blocks release.

1. The first screen is the Fyxed repair contradiction. No generic hero precedes it.
2. The repair changes the composition, so the opening cannot survive a name-and-copy swap.
3. Fyxed copy separates the rent-timing program from the proposed repair thesis.
4. Owner-paid financing is visibly a question to test.
5. FutureClinic uses a different information structure and contains no fake artifact.
6. Cobalt appears only for Sergey's proposed correction.
7. Public state counters, legends, taxonomies, case numbers, and technical theater are absent.
8. The static SSR version contains the complete meaning and no inert visible control.
9. Hydrated keyboard, touch, reduced-motion, print, and focus paths preserve the story.
10. The page contains no invented metric, quote, source, date, product state, or outcome.

## Source checks

- Exactly one `h1` with a clean accessible name.
- `main` has `tabIndex={-1}` for Safari skip-link focus.
- The action button has a 44px target and no toggle or disclosure ARIA.
- Hidden enhanced states are removed from the accessibility tree.
- The base stylesheet shows the complete thesis. Folding is never the default.
- Print forces complete static content and hides the action.
- No legacy `Decision Trace` or `Field Entry` component remains in rendered HTML.
